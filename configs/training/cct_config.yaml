# Configuration for CCT Estimation PINN Training

model:
  input_dim: 8
  hidden_dims: [64, 64, 64]
  activation: tanh
  dropout: 0.0

training:
  batch_size: 32
  num_epochs: 100
  learning_rate: 0.001
  weight_decay: 0.00001

data:
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
